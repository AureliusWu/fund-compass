{
  "name": "sinan-estimate-push-worker",
  "private": true,
  "version": "8.0.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "node scripts/deploy.mjs",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "~5.6.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.0.0"
  }
}
