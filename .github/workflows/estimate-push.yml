name: estimate-push

# 司南基金 · 自选实时估值推送人工应急入口。
# 交易日北京时间 14:30 的正式定时任务已迁移到 Cloudflare Worker。
on:
  # 14:30 定时触发已迁移到 Cloudflare Worker；这里仅保留人工应急入口。
  workflow_dispatch:
    inputs:
      force:
        description: 忽略交易日判断强制推送（测试用）
        type: boolean
        default: false
      slot:
        description: 推送时段（测试用，目前仅支持 14:30；留空则按当前北京时间）
        type: string
        default: ''

permissions:
  contents: write

jobs:
  push:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: 估值推送
        env:
          GIST_TOKEN: ${{ secrets.GIST_TOKEN }}
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
          PUSHPLUS_TOPIC: ${{ secrets.PUSHPLUS_TOPIC }}
          WECHAT_SENDKEY: ${{ secrets.WECHAT_SENDKEY }}
          SC_SENDKEY: ${{ secrets.SC_SENDKEY }}
          NOTIFY_WEBHOOK_URL: ${{ secrets.NOTIFY_WEBHOOK_URL }}
          FUND_API_BASE: ${{ secrets.FUND_API_BASE }}
          FORCE: ${{ inputs.force }}
          PUSH_SLOT: ${{ inputs.slot }}
          SCHEDULE_CRON: ${{ github.event.schedule }}
          PYTHONUNBUFFERED: '1'
        run: python tools/estimate_push.py
