# Changelog

## 6.0.4 - 2026-08-01

- 自选页改为本地自选与缓存估值立即渲染，云同步、行情和决策在后台并行更新。
- 决策摘要按单只基金完成顺序渐进展示，不再等待最慢请求后统一出现。
- 新增带源时间的跨重载估值缓存，缓存值明确降级标记且继续遵守过期隐藏规则。

## 6.0.3 - 2026-07-26

- 首屏改为按需加载 Vant 样式，移除无效字体预连接并压缩 PWA 图标。
- 首页健康检查、市场温度、自选信号和任务状态改为并行加载，普通打开复用任务缓存。
- ECharts 与安装图标退出 Service Worker 安装期预缓存，改为使用时长期缓存。

## Worker 6.0.3 - 2026-07-26

- 新增公开只读 `GET /holdings?code=` 代理，为蜉蝣基金补齐东方财富重仓接口要求的来源标头。
- 返回十大重仓、占净值比例和季度披露截止日期，并区分上游失败与有效空披露。
- 增加代码校验、跨域、缓存、解析和来源标头回归测试。

## 6.0.2 - 2026-07-26

- 非交易日或估值表异常时，Worker 批量回退到最近两条正式净值并返回可追溯日期。
- 自选页将降级值明确显示为“最近净值/净”，不再整列显示“暂无估值”。
- 增加估值源、正式净值源同时失败，以及周末正式净值降级回归测试。

## 6.0.1 - 2026-07-22

- 新增公开只读的批量估值代理，为上游请求补齐浏览器无法设置的来源标头并限制代码数量。
- 自选页改为单次批量请求；代理异常时保留旧值并明确标记，QDII 继续使用既有可审计模型增强。
- 补齐跨域、非法参数、缺失值、上游失败与缓存降级回归测试。

## 6.0.0 - 2026-07-22

- 详情决策接入后端实时估值上下文与用户持仓状态，形成可解释的操作闭环。
- 未持有输出买入、分批定投、观望；已持有输出加仓、持有、减仓、卖出。
- 新增强度、数据状态/时间、相对位置、趋势、操作方式、风险和结论变化条件。
- PWA 对 `/api` 改为 NetworkOnly，浏览器请求使用 `no-store`，避免旧决策静默回退。
- 移除 GitHub Actions 盘中定时信号轮询；估值推送仅保留人工应急，正式任务由 Cloudflare Worker 承担。
- 拆分持仓富集、基金经理富集和策略校准工作流，并强化 CI、Pages 部署与部署后冒烟检查。
