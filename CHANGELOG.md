# Changelog

## 7.0.0 - 2026-08-12

- 盘中估值新增 `holdings_model`：参考蜉蝣基金的公开持仓穿透方法，在司南 Worker 内独立实现，以公开披露持仓、可用行情和最近正式净值生成非官方参考估值，并保留覆盖率、行情时间、持仓披露日期和模型状态证据；批量响应用 `accounting` 对直接盘中、模型、正式净值降级与不可用结果逐项守恒计数。
- **Breaking contract**：`GET /estimates` 的 `est_kind` 不再只有实时估值表与 `official_nav`，客户端必须显式处理 `holdings_model`；该分支使用 `source=eastmoney_holdings_model`、`status=modeled`、`source_time_precision=datetime`、`est_realtime=false`，并通过 `model_coverage`、`model_quote_count`、`model_report_date` 和报价时间范围说明证据。不得仅凭 `est_nav` 为数值就认定它是基金公司净值或官方实时估值。`official_nav` 与 `unavailable` 的降级/空值语义继续保留。
- FundVal v13 兼容：旧客户端不识别新模型，因此 `holdings_model` 的旧别名 `est_time` 故意降为日期级，避免被误标为实时；模型的精确时间以规范 `source_time` 和 `model_*_quote_time` 为准。
- 健壮性收口：第三方 GET 增加超时、有限重试、响应体上限、JSON/结构校验和稳定失败原因；持仓覆盖不足、行情过期、部分报价缺失或结果非有限时失败关闭，不以 0 补缺失数据，不把降级结果送入新鲜盘中推送。
- 后端决策接收估值来源、模型/新鲜度状态和时间证据；只有证据满足门槛的盘中结果参与实时决策，正式净值降级继续明确标记为非实时。
- 修复每周策略校准工作流未安装 `backend/requirements.txt` 导致缺少 `requests` 的问题；自动数据任务提交后显式调度 CI，使持仓、经理、基金全集、海外精度与校准数据经过 CI 后进入 Pages 部署，且不形成 push 循环。
- 部署后烟测绑定源提交中的统一版本，校验 Pages、API、Worker、正式净值与盘中模型/降级联合契约；烟测只调用公开 GET，不要求非交易时段出现 `holdings_model`，也不触发通知。
- 前端、API、Worker、PWA 描述与锁文件统一升级为 7.0.0。Render 免费实例仍为临时 SQLite，生产预期保持 `ephemeral / durable=false`；自然 Cron 成功仍需在部署后的工作日 14:30/14:40 观测。
- 安全配置收紧：`GIST_ID` 不再写入公开的 `wrangler.toml`，改由 Cloudflare/GitHub Secret 注入。历史中曾出现的 Gist ID 必须在生产切换前轮换；GitHub secret Gist 不等同于真正私有存储。
- Gist 轮换恢复采用失败关闭：浏览器命中已删除的旧 ID 后只读重绑定，必须先成功下载并完成 schema 校验/本地合并，才允许再次上传；自选与手工资产分别加写锁，避免旧设备整文件覆盖新 Gist。

## 6.0.6 - 2026-08-09

- 修正 Cloudflare Cron 星期编号：北京时间 14:30/14:40 仅在周一至周五触发，不再误跑周日、漏跑周五。
- Worker 分离自然调度心跳、调度结果/跳过原因、发送尝试与最近成功；无新鲜估值和上游异常也会留下真实状态，异常继续向 Cloudflare 传播。
- 运行状态页展示新的调度契约，并保留旧 Worker 状态兼容；PWA 语言元数据改为 `zh-CN`。
- SQLite 持久化健康状态增加真实挂载点、路径边界和可写性校验，避免仅设置环境变量就误报 `durable=true`。
- 策略校准工作流在未配置仓库 Secret 时使用公开生产 API 默认地址，修复每周任务启动即失败的问题。
- 前端、API 与 Worker 版本统一为 6.0.6；部署后烟测增加 Worker 调度健康字段契约检查。

## 6.0.5 - 2026-08-08

- 缺失净值保持为空并显示覆盖率；未完整定价时暂停总资产、收益、快照、组合权重、诊断与再平衡，避免把缺失值当成 0。
- 基金比较统一使用共同净值日期；组合相关性改为全局共同日期窗口的日收益率 Pearson。QDII 异动区分最新公布净值与下一净值估算，过期缓存不再触发提醒。
- 历史回测禁用当前指数 PE/PB，组合再平衡适配现行动作词，缓存与指数健康接口返回真实数据年龄和降级状态。
- Worker 对每只基金独立校验日期与关键数值，过期/不完整估值逐只回退正式净值；不可用明细与旧数值列表隔离，避免跨项目客户端把空值转成 0。缺 Token/鉴权失败不再静默记为已发送，超时降级会写入警告。
- 人工应急链路停用已退役的 fundgz，改用估值代理并补齐 Worker 鉴权与幂等键；基金列表和详情主源统一强制 HTTPS。
- Pages 部署改为等待整套 CI 成功，部署后烟测校验前端、API、Worker 的目标版本及估值空值契约。
- 前端、API 与 Worker 版本统一为 6.0.5；运行状态明确显示 SQLite 是否为持久盘，Render 免费实例继续标记为临时存储。

## 6.0.4 - 2026-08-01

- 自选页改为本地自选与缓存估值立即渲染，云同步、行情和决策在后台并行更新。
- 决策摘要按单只基金完成顺序渐进展示，不再等待最慢请求后统一出现。
- 新增带源时间的跨重载估值缓存，缓存值明确降级标记且继续遵守过期隐藏规则。

## 6.0.3 - 2026-07-26

- 首屏改为按需加载 Vant 样式，移除无效字体预连接并压缩 PWA 图标。
- 首页健康检查、市场温度、自选信号和任务状态改为并行加载，普通打开复用任务缓存。
- ECharts 与安装图标退出 Service Worker 安装期预缓存，改为使用时长期缓存。

## Worker 6.0.3 - 2026-07-26

- 新增公开只读 `GET /holdings?code=` 代理，为蜉蝣基金补齐东方财富重仓接口要求的来源标头。
- 返回十大重仓、占净值比例和季度披露截止日期，并区分上游失败与有效空披露。
- 增加代码校验、跨域、缓存、解析和来源标头回归测试。

## 6.0.2 - 2026-07-26

- 非交易日或估值表异常时，Worker 批量回退到最近两条正式净值并返回可追溯日期。
- 自选页将降级值明确显示为“最近净值/净”，不再整列显示“暂无估值”。
- 增加估值源、正式净值源同时失败，以及周末正式净值降级回归测试。

## 6.0.1 - 2026-07-22

- 新增公开只读的批量估值代理，为上游请求补齐浏览器无法设置的来源标头并限制代码数量。
- 自选页改为单次批量请求；代理异常时保留旧值并明确标记，QDII 继续使用既有可审计模型增强。
- 补齐跨域、非法参数、缺失值、上游失败与缓存降级回归测试。

## 6.0.0 - 2026-07-22

- 详情决策接入后端实时估值上下文与用户持仓状态，形成可解释的操作闭环。
- 未持有输出买入、分批定投、观望；已持有输出加仓、持有、减仓、卖出。
- 新增强度、数据状态/时间、相对位置、趋势、操作方式、风险和结论变化条件。
- PWA 对 `/api` 改为 NetworkOnly，浏览器请求使用 `no-store`，避免旧决策静默回退。
- 移除 GitHub Actions 盘中定时信号轮询；估值推送仅保留人工应急，正式任务由 Cloudflare Worker 承担。
- 拆分持仓富集、基金经理富集和策略校准工作流，并强化 CI、Pages 部署与部署后冒烟检查。
