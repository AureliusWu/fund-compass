# 司南基金 · 长期迭代计划

> 本文件是项目的**单一事实来源**：下一步做什么看这里，做完更新这里。配套阅读 [../CLAUDE.md](../CLAUDE.md)（工作流与铁律）。
> 维护方式：每次迭代——从「下一步队列」取最上面一项，完成后勾掉并在文末「迭代日志」记一行。

## 北极星 & 边界

- **北极星**：让个人用户「每天打开就能快速知道——哪些基金值得关注、当前适不适合买」。
- **边界**：个人辅助、非机构量化、非自动交易、非宏观预测。任何取舍都向「准确、可解释、轻量、中立」靠拢。

## 优先级原则

按 **价值 ÷ 工作量** 排序，且遵循：**工程地基/技术债 ＞ 核心算法准确度 ＞ 新功能 ＞ 体验打磨**。
理由：没有测试网时改算法极易回归；估值/信号「准不准」是产品立身之本，比多一个页面更重要。

---

## 下一步队列（从上往下做）

### P0 · 工程地基与技术债（先做，给后续改动兜底）

- [x] **前端单测**（✅ 2026-07-04）：引入 vitest，接入 CI；已覆盖 `format`、`dca`、`estimate`、`screener`、`attribution`、`lookthrough`，前端单测 52 用例全绿。
  - 价值高·工作量中。验收：关键计算函数有测试，CI 跑前端单测。
- [x] **可观测性**（✅ 2026-06-29）：eastmoney/repo/main 的静默吞错改结构化 `logging`——主源降级、退陈旧缓存、启动导入失败均可见，字段级容错保持静默不刷屏。
  - 价值中·工作量小。验收：数据源异常/降级在日志可见，便于线上定位。
- [x] **数据源健壮性**（✅ 2026-06-29）：`eastmoney.source_health()` 进程内统计主源成功/失败/失败率/最近错误/疑似降级（degraded），经 `/api/health` 暴露。
  - 价值中·工作量中。验收：主源解析异常能被发现，不再「无声失准」。
  - 注：进程内计数（重启清零、多 worker 各自独立），单实例部署够用；要跨实例/历史趋势需外部监控。

### P1 · 核心算法准确度

- [~] **V3-5 真实 PE/PB 估值（命门）**：建「基金 ↔ 跟踪指数」映射表，取指数真实 PE/PB 历史分位替换择时估值层现有的「去趋势净值分位」代理；非指数基金优雅降级到代理。**分两步推进：**
  - [x] 步骤1 数据管线（✅ 2026-06-29，经 4 轮 CI 验证）：`tools/enrich_index_valuation.py` 用乐咕乐股 `stock_index_pe_lg`/`pb_lg`（akshare 1.18.64 已移除 funddb 系列）取 6 个主流宽基（沪深300/上证50/上证180/中证100/中证500/中证1000）市值加权 PE/PB + 自算历史分位 → `index-valuation.json`；`fund_index_map.json` 种子映射；接入 `enrich.yml`（continue-on-error，第三方源不阻断其他富集）。**数据源已验证可用、数值合理。**
  - [x] 步骤2 接入（✅ 2026-06-30）：后端 `strategy/index_valuation.py` 模块导入时加载 `index-valuation.json` + `fund_index_map.json` 到内存；`timing.valuation_layer` 对已映射指数基金优先用 PE 分位（<30 低估/30-70 合理/>70 高估），非指数基金回退去趋势代理（标记 `source: "index_pe_pb"` vs `"nav_detrended"`）；前端详情页/报告页/解读模板展示 PE/PB 数值与指数名。新增 `test_index_valuation.py`（8 用例）+ `test_timing.py` 补 6 用例。后端 pytest 49 全绿，前端 type-check+build 全绿。
  - 价值高·工作量高（难在免登录的指数估值数据源）。验收：指数基金估值层用真实分位，强趋势长牛被误判高估的问题缓解；回测踏空改善；非指数基金不报错。

### P2 · 功能打磨

- [x] **V3 落地质量巡检**（✅ 2026-06-30）：逐项核实 V3-0~V3-10 十大功能。结论：九项完整落地；仅 V3-9 多源容灾有缺口——`resilience.ts` 框架（SWR/熔断/重试）已编写但未注入 `indices.ts`/`holdings.ts`/`estimate.ts` 的实际请求。已修复：在三文件中注入 `recordSource()` 调用，HomePage 源状态点灯现在可真实反映腾讯行情、东方财富、天天基金三源健康度。
- [x] **盘中「按持仓重算估值」**（✅ 2026-07-04）：`estimate.ts` 新增通用十大重仓穿透模型，定向模型缺失时按 `holdings.ts` 的公开持仓把 A股/港股/美股映射到腾讯行情并按占净值比例加权估算；保留 012920/018147 等定向模型优先，报告页同步使用最新净值涨跌/下一净值模型双口径。
  - 价值中·工作量中。注意：精度上限受「仅前十大重仓」限制，性价比中等，排在巡检之后。

### P3 · 体验与运维

- [x] 体验细节（✅ 2026-07-04）：首页 / 自选持仓 / 基金详情接入下拉刷新；自选持仓与基金详情加载态从纯转圈升级为结构化骨架屏；错误态优化——FundDetailPage 加载失败增加「重试」按钮（✅ 2026-06-30）。
- [x] 运维可见性（✅ 2026-07-04）：`/api/health` 新增 `index_valuation` 字段（数据更新时间 + 覆盖指数数）（✅ 2026-06-30）；首页系统状态接入 GitHub Actions 公开 API，展示离线富集 / 估值推送 / 信号保活最近运行状态、运行间隔与异常点灯。

### P4 · 资产分析深化

- [x] 组合历史自动快照（✅ 2026-07-04）：资产页在持仓市值加载完成后每日自动记录一次组合市值/成本快照；手动「拍快照」仍可覆盖当天数值；两天以上自动展示组合历史曲线。新增 `snapshots.test.ts` 覆盖同日覆盖、自动不覆盖、空资产不记录、图表生成边界。

### P5 · 估值准确度继续补强

- [x] **真实 PE/PB 覆盖扩展**（✅ 2026-07-04）：在现有沪深300/上证50/上证180/中证100/中证500/中证1000 基础上，富集脚本新增创业板、科创、红利、消费/白酒、医药、证券、半导体候选 symbol；基金映射补齐常见指数 ETF/联接；恒生科技、纳指、标普等海外/港股指数在未接入稳定免登录历史 PE/PB 源前明确降级说明。
  - 价值高·工作量中高。验收：更多指数基金详情页/报告页展示 `index_pe_pb` 真实估值；新增映射和加载测试；第三方数据失败不阻断富集任务。

### P6 · 收益归因深化

- [x] **本月/近30日收益归因**（✅ 2026-07-04）：组合快照扩展可选持仓明细；资产页在每日/手动快照中记录持仓明细；收益归因新增近30日区间收益、按账户/类型拆解、最佳/最弱贡献项；缺少两条含明细快照时显示明确空态。
  - 价值中高·工作量中。验收：至少两条快照后能展示区间收益、贡献占比、最佳/最差贡献项；补归因单测。

### P7 · 跨账户数据模型收口

- [x] **同一基金跨多账户稳定化**（✅ 2026-07-04）：复合键账户名 trim 归一化；自选代码级删除会 tombstone 该基金所有账户行，同时保留按账户精确删除能力；补 store/gist 测试覆盖旧数据迁移、跨账户独立记账、全账户删除、单账户删除。
  - 价值中·工作量中高。验收：同一基金在不同账户可独立记账、同步、删除、导出；旧 `sinan_watchlist_v1` 自动迁移；新增 store/gist 测试。

### P8 · 体验工程收尾

- [x] **导出与暗黑体验补齐**（✅ 2026-07-04）：CSV 生成函数测试化，新增组合快照与近30日归因导出；资产页提供快照/归因导出按钮；资产页/详情页进度轨道与关键文字改用主题变量，降低暗黑模式低对比风险。
  - 价值中·工作量中。验收：移动端暗黑无明显低对比；导出的 CSV 可被 Excel 正确打开；关键页面截图人工确认。

### P9 · 大类资产记账

- [x] **现金 / 股票 / 黄金纳入资产页**（✅ 2026-07-04）：新增手工资产本地存储与 Gist JSON 同步；资产页可新增/编辑/删除/上传/下载现金、股票、黄金等非基金资产，并纳入总资产、大类配置、快照与近30日归因。
  - 价值中·工作量高。验收：手工资产可新增/编辑/删除/同步；资产页总市值、配置、诊断同时纳入基金与非基金资产。

### V7 · 下一轮计划：让“自迭代”经得起实盘

- [x] **V7-P0 · 首轮线上校准审计**（✅ 2026-07-09）
  - 手动运行跨基金校准任务，核对有效样本、类型分布、候选支持率、验证超额和回撤门槛；确认注册表晋级/拒绝/回滚链路与页面展示一致。
  - 验收：留存首份真实校准报告；失败可解释，成功可回滚，不因单一类型基金主导而晋级。
  - 首轮结果：30 只均衡覆盖 6 类（每类 5 只），26 只有效；同一候选仅获 2 票、门槛 11 票，验证超额中位数提升 0.46pct、低于 0.5pct，候选正确拒绝，active 保持 `v1-default`。晋级门槛追加“至少 4 类有效、单类占比不超过 40%”。
- [x] **V7-P1 · 回测摩擦成本与稳健性**（✅ 2026-07-09）
  - 回测加入申赎费、滑点、现金收益与最短持有期；增加不同市场阶段、参数扰动和滚动窗口压力测试。
  - 验收：无摩擦/有摩擦结果并列展示；候选参数在合理扰动下不出现性能断崖。
  - 已实现：默认纳入申购 0.15%、赎回 0.5%、滑点 0.02%、现金年化 1.2% 与最短持有 1 月；返回无摩擦对照、换仓损耗，并以双倍费率/更高滑点执行压力测试。
- [x] **V7-P2 · 决策结果闭环**（✅ 2026-07-09）
  - 每日记录当时决策、参数版本和后续 5/20/60 日表现，形成“建议之后发生了什么”的可追溯样本。
  - 验收：可按动作、置信度、基金类型统计命中率、超额和回撤，不使用事后数据改写历史决策。
  - 已实现：批量决策写入不可覆盖的 `decision_history` 快照，记录当时净值、动作、置信度与策略版本；`/api/strategy/outcomes` 仅使用决策日之后第 5/20/60 个净值观测计算收益与动作命中率。
- [x] **V7-P3 · Champion / Challenger 漂移监控**（✅ 2026-07-09）
  - 当前线上参数作为 Champion，候选作为 Challenger 并行评估；检测数据源漂移、类型失衡和连续退化，触发冻结晋级或自动回滚。
  - 验收：健康接口和首页状态可见；连续两个周期退化时自动冻结，达到回滚门槛时恢复上一稳定版本。
  - 已实现：注册表健康信息经 `/api/health` 与 `/api/strategy/registry` 暴露；样本不足或类型失衡冻结晋级；同一 active 版本在成熟的 20/60 日结果中至少两个分组命中率低于 40% 记为一次退化，连续两个周审计周期后自动恢复最近历史版本。
- [x] **V7-P4 · 发布与运维收口**（✅ 2026-07-09）
  - 校准任务增加报告 artifact、失败告警和运行时长预算；补 API 契约测试、移动端截图回归及部署后 smoke test。
  - 验收：CI、定时任务、Pages 与后端部署均有可追踪结果；发布失败不污染 active 参数。
  - 已实现：校准任务上传 90 天 artifact 并写 GitHub Summary；新增 CI 成功后的 Pages/后端 smoke workflow；校准脚本仅在完整跑完后原子更新注册表，失败保持旧 active；版本升至前端 5.1.0 / API 0.4.0。

### V8 · 下一轮计划：让实盘精度可衡量、可解释、可持续提升

V7 已完成受约束自迭代闭环，但首轮校准只有 26 个有效样本，候选支持率与提升幅度均未达到晋级门槛；同时，结果统计尚未完整覆盖基金类型/置信度，海外估值也缺少“当时预测 → 后续实际净值”的独立误差账本。V8 先补真实证据，再迭代算法。

- [x] **V8-P0 · V7 线上发布验收与基线冻结**（✅ 2026-07-10）
  - 核对 CI、Pages、后端健康接口、部署后 smoke、校准 artifact 和策略注册表；将 `v1-default`、首轮 30/26 样本校准报告及当前海外模型参数保存为 V8 对照基线。
  - 验收：线上版本、active 参数、校准报告三者一致；部署失败可定位；后续 V8 结果均能与冻结基线比较。
  - 已实现：核对 Pages、API 0.4.0、`v1-default` 与 30/26 首轮报告，冻结 `v8-baseline.json`；定位并修复 post-deploy smoke 在 job `if` 直接引用 secret 导致无 job 日志失败的问题。
- [x] **V8-P1 · 决策结果统计与实盘看板**（✅ 2026-07-10）
  - 完善 `/api/strategy/outcomes`，按策略版本、动作、置信度、基金类型和 5/20/60 日周期统计样本量、命中率、收益、超额与回撤；前端增加实盘结果页，明确区分“样本未成熟”和“表现退化”。
  - 验收：历史决策不可回写；统计只使用决策日之后的数据；任一分组可追溯到原始决策，空样本和小样本不输出误导性结论。
  - 已实现：结果增加同类横截面超额与路径回撤，按策略版本/动作/置信度/基金类型分组；新增 `/outcomes` 实盘验证页，成熟与等待样本分开展示。
- [x] **V8-P2 · 海外估值误差账本**（✅ 2026-07-10）
  - 定时保存 018147、012920 及其他 QDII 的预测值、模型版本、行情时间、持仓覆盖率和数据源；实际净值公布后按对应交易日自动配对，计算 MAE、偏差、方向命中率和分位误差，避免把延迟净值错配到预测日。
  - 验收：可逐日回答“当时估多少、实际多少、误差多少”；7 月 3 日等历史异常日可复盘；数据缺失、节假日和跨时区场景有明确状态。
  - 已实现：14:30 任务记录模型版本、行情时间、因子值、覆盖率与预测；只按精确净值归属日结算。7 月 3 日可见的 018147 -12.09% / 012920 -8.01% 归属 7 月 2 日，作为 observed-only 复盘记录，不进入训练。
- [x] **V8-P3 · 海外模型受约束自校准**（✅ 2026-07-10）
  - 基于 P2 误差账本对持仓、地区指数、行业/风格因子权重做滚动训练和时间序列留出验证；采用单基金 Challenger + 同类回退模型，只有样本量、MAE、方向命中率和极端日误差同时过门槛才晋级。
  - 验收：训练严格早于验证期；018147、012920 分别给出相对 V8 基线的改善；连续退化自动冻结并回滚，不因一次大涨跌日过拟合。
  - 已实现：海外模型配置统一注册；逐基金 70/30 时间留出，训练段受限搜索因子权重/scale/bias，验证段同时约束 MAE、方向和极端日；20 样本前仅 collecting，连续两周期退化自动回滚历史 active。
- [x] **V8-P4 · 用户可见的可信度与误差区间**（✅ 2026-07-10）
  - 在自选、详情和推送中展示估值日期、数据新鲜度、持仓覆盖率、模型状态、历史误差区间与可信度；QDII 明确区分“最新已公布净值”“对应交易日模型估值”和“当日实时行情推算”。
  - 验收：用户无需理解算法即可判断估值是否新鲜、为何可信或为何仅供参考；移动端不增加信息拥挤和横向滚动。
  - 已实现：自选与详情展示模型版本、持仓/因子覆盖率、样本可信度和 P80 历史误差区间；实盘验证页展示海外精度与 7 月异常日归属口径。
- [x] **V8-P5 · 自动化验收与版本发布**（✅ 2026-07-10）
  - 为误差配对、跨时区日期、校准防泄漏、晋级/冻结/回滚补测试；定时任务输出精度周报 artifact，发布前执行后端、前端、移动端和线上 smoke 全套门禁。
  - 验收：全量测试通过；精度周报可追踪；发布失败不污染预测账本或 active 模型；完成后统一升级前端/API 版本。
  - 已实现：误差账本/日期配对/时间留出/漂移测试进入 CI；每日精度 artifact 与 GitHub Summary；后端 89、前端 79 项测试全绿，type-check/build/移动端 390×844 验证通过；版本升至前端 5.2.0 / API 0.5.0。

**执行顺序：** P0 → P1 → P2 → P3 → P4 → P5。P2 是 V8 的数据地基，P3 不在 P2 积累出足够成熟样本前自动晋级；样本不足时只运行 Challenger 并持续收集证据。

### V9 · 证据质量与精度监控

- [x] **V9-P0 · 海外净值日期状态机**（✅ 2026-07-10）
  - 为预测记录增加 pending / settled / stale / market-closed 状态、等待天数与结算说明；只接受精确净值归属日，禁止用“下一条净值”错配。
  - 验收：跨周末、海外休市、净值延迟超过 7 天均可解释；重复运行幂等，不污染训练样本。
  - 已实现：pending/settled/stale/market_closed 四态与等待天数；stale 后仍只接受精确归属日结算，绝不拿下一条净值顶替。
- [x] **V9-P1 · 决策同类基准去偏**（✅ 2026-07-10）
  - 同类超额改为同类型、相近决策日期、相同周期的 leave-one-out 基准，记录同类样本数与覆盖率。
  - 验收：自身收益不进入自身基准；日期相隔过远不比较；不足 2 个同类样本时不输出伪精确超额。
  - 已实现：同类型、决策日 ±7 天、相同周期的 leave-one-out 基准；响应附基准样本数和方法，至少两个外部同类才算超额。
- [x] **V9-P2 · 精度趋势与分位监控**（✅ 2026-07-10）
  - 海外精度报告增加滚动 5/20 样本 MAE、偏差、方向命中率及 P50/P80/P95 误差；实盘验证页展示趋势和近期漂移。
  - 验收：趋势严格按结算时间排序；样本不足明确展示 collecting；异常日可定位到原始记录。
  - 已实现：逐基金滚动 5/20 样本 MAE、偏差、方向命中与 P50/P80/P95 绝对误差；实盘验证页同步展示待结算和异常等待数。
- [x] **V9-P3 · 数据质量告警与周报**（✅ 2026-07-10）
  - 增加海外审计脚本，检查长时间 pending、行情覆盖不足、日期倒置、重复记录和连续退化；Actions Summary 与 artifact 输出分级告警。
  - 验收：结构错误阻断提交，数据不足仅告警不失败；告警包含基金代码、记录日期与处置建议。
  - 已实现：`audit_overseas_accuracy.py` 检查重复、日期倒置、结算缺字段、低覆盖与漂移；错误退出 1，stale/冻结仅 warning；报告进入 artifact。
- [x] **V9-P4 · 可审计导出**（✅ 2026-07-10）
  - 实盘验证页支持导出决策结果与海外误差 CSV，字段包含日期口径、模型版本、预测、实际、误差和状态。
  - 验收：Excel 可直接打开；observed-only 与 settled 明确区分；不导出密钥或内部请求信息。
  - 已实现：实盘验证页导出决策结果和海外误差 CSV，保留净值归属日、模型版本、预测/实际/误差、等待天数与状态。
- [x] **V9-P5 · 发布门禁**（✅ 2026-07-10）
  - 补日期状态、leave-one-out、趋势分位、审计告警和导出测试；升级前端/API 版本并完成移动端与线上 smoke。
  - 验收：全量测试、类型检查、构建和 390×844 页面检查通过。
  - 已实现：V9 日期、基准、趋势、审计与导出测试进入全量门禁；与 V10 合并发布为前端 5.4.0 / API 0.6.0。

### V10 · 组合级决策实验室

- [x] **V10-P0 · 组合历史回测**（✅ 2026-07-10）
  - 新增组合回测 API：按共同交易日对齐多基金净值，比较月度再平衡、买入持有与现金基准，纳入交易摩擦。
  - 验收：禁止未来数据；权重和为 100%；返回收益、波动、最大回撤、换手成本和净值曲线。
  - 已实现：`POST /api/portfolio/lab` 按共同区间向前填充历史净值，比较月度再平衡、买入持有和现金；返回收益、年化、波动、回撤、换手与摩擦。
- [x] **V10-P1 · 风险贡献与集中度**（✅ 2026-07-10）
  - 基于历史收益协方差计算组合波动、各基金边际风险贡献、相关性集中度与有效持仓数。
  - 验收：风险贡献合计约 100%；零波动/历史不足可降级；不把资产金额大小误当风险本身。
  - 已实现：纯 Python 协方差矩阵、组合波动、边际风险贡献、相关性集中度和有效持仓数；历史不足 60 点明确拒绝。
- [x] **V10-P2 · 受约束再平衡建议**（✅ 2026-07-10）
  - 结合目标仓位、风险贡献、单基金上限和最小调仓阈值生成分步建议，展示预计风险变化与摩擦成本。
  - 验收：不建议负仓位或超过 100%；小偏差不交易；每条建议可解释且不执行真实交易。
  - 已实现：单基金上限、2% 最小调仓阈值、风险贡献 40% 加仓限制；输出金额、成本及调整前后预计波动，输入权重自动归一。
- [x] **V10-P3 · 组合实验室页面**（✅ 2026-07-10）
  - 从现有自选持仓一键载入组合，展示三条回测曲线、风险贡献、压力情景和再平衡清单；支持手动调整目标权重后重算。
  - 验收：空持仓、单基金、历史不足均有明确状态；移动端不横向溢出。
  - 已实现：资产页进入组合实验室，一键载入现有持仓；展示三条历史曲线、风险贡献、再平衡和三种压力情景；390×844 实机验证无溢出。
- [x] **V10-P4 · 组合决策结果留痕**（✅ 2026-07-10）
  - 保存组合级建议快照、策略版本与后续 20/60 日组合表现，和单基金结果共用不可回写原则。
  - 验收：快照可追溯到当时成分与权重；后续表现只使用快照日之后净值。
  - 已实现：组合决策保存不可覆盖的成分、权重、基准净值和策略版本；`/api/strategy/portfolio-outcomes` 只用快照后第 20/60 个观测结算。
- [x] **V10-P5 · 总验收与发布**（✅ 2026-07-10）
  - 补组合对齐、防泄漏、风险贡献、再平衡约束、结果留痕测试；升级版本，完成 CI、Pages、API 与 smoke 验收。
  - 验收：组合算法测试覆盖关键边界；旧资产页、单基金回测和海外估值无回归。
  - 已实现：组合日期对齐、权重上限、风险贡献、压力情景和快照结果测试进入 CI；后端 97、前端 80 项测试首轮全绿，最终门禁见本轮发布记录。

**执行顺序：** V9-P0 → P5，再进入 V10-P0 → P5。V10 只使用 V9 审计通过的数据；实盘样本不足不阻塞功能发布，但所有模型晋级继续保持冻结。

### V11 · 真实样本积累与可信度收口

- [x] **V11-P0 · 海外精度流水线解耦**（✅ 2026-07-10）
  - 新增独立 `overseas-accuracy.yml`，工作日 14:35 执行预测落账、同日净值精确结算、Challenger 校准与审计；不再依附 14:30 推送任务。
  - 账本写入最近运行、预测、结算时间及行情/详情覆盖数量；同基金、日期、版本幂等，周末不生成训练记录。
- [x] **V11-P1 · 精简可信度与日期口径**（✅ 2026-07-10）
  - 自选仍只有“今日决策摘要”和“盘中估值”两区；海外估值行内展示净值基准日、持仓/因子覆盖率、样本状态、样本数与 P80 历史误差。
- [x] **V11-P2 · 展示契约自动测试**（✅ 2026-07-10）
  - 固化三项主导航、两项自选区域、综合温度、已读提醒消失、QDII 新鲜度与可信度、决策理由展示规则。
- [x] **V11-P3 · 数据新鲜度门禁**（✅ 2026-07-10）
  - 行情源按最后检查时间降级红黄绿状态；模型估值超过 24 小时不再展示精确百分比，海外精度报告超过 72 小时标记过期。
- [x] **V11-P4 · 字体与决策摘要收口**（✅ 2026-07-10）
  - 移除 Google Fonts 线上依赖，改用系统字体栈；决策摘要在原区域内增加置信度和首要依据，不新增卡片或页面。
- [x] **V11-P5 · 发布门禁**（✅ 2026-07-10）
  - 前端升级 5.6.0；后端 99、前端 87 项测试首轮通过，最终 CI、Pages、线上 smoke 与首笔预测落账见本轮发布记录。

**V11 约束：** 018147、012920 等基金累计少于 20 条已结算样本时只收集证据，不扩大参数搜索、不自动晋级；隐藏功能保留但不新增入口。

### V12 · 可信度与韧性收口

- [x] **V12-P0 · 收益与评分口径**（✅ 2026-07-11）
  - 主数据源的日收益先复合为累计收益序列；评分增加版本、覆盖率、有效权重和 70% 可用门槛。
- [x] **V12-P1 · 数据新鲜度门禁**（✅ 2026-07-11）
  - 后端过期缓存最多回退 7 天并透传年龄；过期指数 PE/PB 不参与信号；市场温度全源失败时返回旧数据或不可用，不伪造新鲜 50 分。
- [x] **V12-P2 · 超时与错误恢复**（✅ 2026-07-11）
  - 前端 API 统一 12 秒截止并区分超时、网络和 HTTP 错误；第三方连接/读取超时收紧；应用级异常提供刷新恢复入口。
- [x] **V12-P3 · 缓存与缺失值语义**（✅ 2026-07-11）
  - 内存、localStorage、SWR 和 Service Worker 缓存统一版本及有效期；回测、评分与报告不再把缺失值画成 0。
- [x] **V12-P4 · 请求与静态资源性能**（✅ 2026-07-11）
  - 首页提醒复用信号与估值，自选估值和决策并行且不再逐只下载完整详情；排行与基金经理索引改为清单加分片。
- [x] **V12-P5 · 组合日期与发布门禁**（✅ 2026-07-11）
  - 组合回测仅使用共同净值日期；完成后端、前端、类型、构建和浏览器验收，版本升至前端 5.7.0 / API 0.7.0。

### V13 · 数据身份与交互一致性

- [x] **V13-P0 · 经理稳定身份**（✅ 2026-07-11）
  - 保存公开经理 ID，详情页跳转时按 ID 精确定位并展开在管基金；姓名仅作为旧缓存兜底。
- [x] **V13-P1 · 提醒事件指纹**（✅ 2026-07-11）
  - 净值异动使用基金、行情时间和涨跌值组成指纹，信号提醒使用前后信号组成指纹；36 小时内同事件只生成一次。
- [x] **V13-P2 · 旧提醒持久化迁移**（✅ 2026-07-11）
  - 读取旧版 localStorage 时折叠重复提醒并立即写回，修复重复展示和重复系统通知。
- [x] **V13-P3 · 自选布局契约**（✅ 2026-07-11）
  - 估值区使用统一响应式列宽 token；海外模型多行说明与普通估值共享数值右边界，移动端固定为 132px。
- [x] **V13-P4 · 元数据迁移与发布**（✅ 2026-07-11）
  - SQLite 自动增加 `manager_id`，API 类型同步；完成全量测试、浏览器桌面/移动验收，版本升至前端 5.8.0 / API 0.8.0。

---

## 版本节奏

- 按 ROADMAP 编号推进，每项**可独立验收、可演示**即可交付，不强求固定发布周期。
- 提交走语义化中文 commit；积累若干功能后再在 `frontend/package.json`（前端 5.x）与 `backend/main.py`（API version）统一升版本号。
- 每次合入 `main` 前：后端 `pytest` 全绿 + 前端 `type-check` & `build` 通过（CI 已自动校验）。

---

## 迭代日志（最近在前）

- **2026-07-10**
  - V11：海外精度账本从估值推送解耦，增加运行元数据与幂等/周末门禁；自选补回精简可信度、数据过期门禁与决策首要依据；移除 Google Fonts 依赖。
  - V10：组合实验室完成——组合历史回测、风险贡献、有效持仓、相关集中度、受约束再平衡、压力情景与组合建议 20/60 日留痕。
  - V9：证据质量完成——海外日期状态机、leave-one-out 同类基准、滚动误差分位、质量审计告警与实盘 CSV 导出。
  - V8 全部完成：实盘决策结果看板、海外估值逐日误差账本、018147/012920 受约束自校准、可信度/误差区间展示、漂移冻结回滚与自动化发布门禁落地。
  - 明确海外净值日期口径：展示日不等于净值归属日；7 月 3 日所见两次大跌实际归属 7 月 2 日，历史复盘记录不参与训练。
- **2026-07-07**
  - 回测(V6-P7)：回测实验室展示单基金校准状态、当前/候选验证超额及拒绝原因；自迭代链路完成产品化收口。
  - 回测(V6-P6)：新增跨基金校准任务 `calibrate_strategy.py`、策略参数注册表与每周 Actions；至少 12 个有效样本、候选获 40% 支持且验证超额中位数提升 ≥0.5pct 才自动晋级，失败保留上一 active 版本；相同参数不重复发版，晋级时保留最近 10 个历史版本供回滚。
  - 回测(V6-P5)：新增受约束参数自校准 `calibration.py` 与 `/api/fund/{code}/calibrate`；训练段从有限候选中选择信号仓位参数，最后 30% 历史留出验证，只有超额收益与回撤门槛同时通过才标记可进入候选版本，绝不静默覆盖线上规则。
  - 决策(V6-P3)：14:30 推送从逐只决策请求改为批量组合决策；按 Gist 跨账户持仓与盘中估值计算当前仓位，推送加入目标现金、仓位差与前三项调仓优先级；GitHub Actions 补传 `FUND_API_BASE`，后端异常自动降级纯估值。
  - 决策(V6-P2)：批量决策新增组合校准摘要与调仓优先级，检查目标仓位合计、目标现金余量、超配/低配；自选页展示建议仓位差和参考金额，未设置目标的基金自动均分剩余额度。
  - 决策(V6-P1)：新增 `POST /api/portfolio/decisions` 批量决策；自选页展示「今日决策摘要」与每只基金的决策动作；持仓编辑支持目标仓位 %。
  - 决策(V6-P0)：新增 `decision.py` + `rules.py` 决策引擎，`GET /api/fund/{code}/decision`；`/analyze` 聚合决策卡片；回测透传 `code/type` 使 PE/PB 映射生效；前端 `DecisionCard.vue` 在详情页「智能解读」前展示动作/置信度/依据/风险/仓位规则。补 `test_decision.py`。
- **2026-07-04**
  - 资产(P9)：大类资产记账落地——新增 `manualAssets.ts` 管理现金/权益/商品手工资产，本地存储并通过同一 Gist 的 `sinan-manual-assets.json` 同步；资产页支持新增/编辑/删除/上传/下载，手工资产纳入总资产、大类配置、快照和近30日归因；`assetclass.ts` 新增“商品”类。补 `manualAssets.test.ts`、`assetclass.test.ts`。
  - 体验(P8)：导出与暗黑收尾——`export.ts` 暴露 `toCSV` 并新增快照/近30日归因 CSV 导出；资产页增加导出按钮；修复资产/详情页固定浅色进度轨道和部分硬编码文字色；新增 `export.test.ts` 覆盖 CSV 转义。
  - 数据模型(P7)：跨账户持仓收口——`entryId` 对账户名 trim 归一化；`watchlist.remove(code)` 改为删除该基金所有账户行，`remove(code, account)` 保留单账户删除；新增 `watchlist.test.ts` 覆盖迁移、复合键、删除墓碑与独立账户持仓。
  - 归因(P6)：资产页新增近30日区间归因——快照持仓明细化，`computePeriodAttribution` 按基金/账户/类型拆解区间资产变动贡献，快照不足时空态说明；补 `attribution.test.ts` 与 `snapshots.test.ts`。 
  - 准确度(P5)：扩展真实 PE/PB 覆盖——`enrich_index_valuation.py` 支持多候选 symbol 逐个尝试，新增创业板/科创/红利/消费/白酒/医药/证券/半导体候选；`fund_index_map.json` 补常见指数 ETF/联接映射；`index_valuation.py` 支持指数别名归一化与 `unavailable_reason`，海外/港股 PE/PB 未覆盖时详情页回退说明更具体。后端 pytest 52 ✅，前端 vitest 63 ✅、type-check ✅、build ✅。
  - 资产(P4)：组合历史从“手动拍快照”升级为“每日自动快照”——新增 `takeDailySnapshot`，资产页加载完成后自动记录当日市值/成本且不覆盖用户手动快照；补 `snapshots.test.ts` 4 用例。
  - 运维(P3)：补齐定时任务运行状态可观测——新增 `taskStatus.ts` 读取 GitHub Actions workflow 最近一次运行，首页系统状态展示离线富集、估值推送、信号保活的成功/失败/过期点灯与最近运行时间；新增 `taskStatus.test.ts` 覆盖成功、过期、失败、无记录口径。前端 `vitest` 59 ✅、`type-check` ✅、`build` ✅。
  - 体验(P3)：补齐移动端刷新与加载体验——首页、自选持仓、基金详情接入 `van-pull-refresh`；自选持仓/详情页初次加载改为 `van-skeleton` 骨架屏。前端 `type-check` ✅、`vitest` 55 ✅、`build` ✅。
  - 地基(P0)：补齐前端关键工具函数单测——新增 `screener.test.ts`、`attribution.test.ts`、`lookthrough.test.ts`，覆盖排行缓存/同类更优、收益归因、持仓穿透 enrich/top10/mixed/none 降级口径。前端 vitest 从 37 扩到 52 用例，`npm run test` ✅、`type-check` ✅、`build` ✅。P0「前端单测」收口完成。
  - 功能(P2)：盘中「按持仓重算估值」系统化——新增 `holdingsToOverseasModel`，定向海外模型缺失时自动用公开十大重仓生成穿透模型；支持 A股/港股/美股/韩国代理映射，低于 25% 可用权重时保守降级。报告页同步使用 `preferredDailyMove`，避免海外模型冒充最新净值涨跌。新增 estimate 单测 3 个。
- **2026-06-30**
  - 功能(P1/V3-5 步骤2)：真实 PE/PB 估值接入——新建 `backend/strategy/index_valuation.py` 加载器（模块级缓存、优雅降级）；`timing.valuation_layer` 对已映射指数基金优先用 PE 分位，非指数基金回退去趋势代理（新增 `source` 字段区分）；`timing_signal` 透传 `detail["code"]`。前端 `Layer` 类型扩展 PE/PB 字段，详情页/报告页/解读模板展示真实 PE/PB 与指数名。新增 `test_index_valuation.py`（8 用例）+ `test_timing.py` 补 6 用例（49 全绿）。
  - 巡检(P2/V3 落地质量)：逐项核实 V3-0~V3-10 十大功能——九项完整落地，仅 V3-9 多源容灾有缺口。修复：在 `indices.ts`/`holdings.ts`/`estimate.ts` 注入 `recordSource()` 调用，腾讯行情/东方财富/天天基金三源状态现在真实反映到 HomePage 源状态点灯。
  - 体验(P3)：FundDetailPage 加载失败增加「重试」按钮；`/api/health` 新增 `index_valuation` 数据新鲜度字段。
  - 质量门禁：后端 pytest 49 ✅ · 前端 type-check ✅ · build ✅ · vitest 28 ✅。
- **2026-06-29**
  - 修复：详情 `source`（primary/fallback）持久化——`fund_detail` 加 `source` 列 + 旧库轻量迁移（`db._migrate`），`repo._save_detail` 写入，缓存命中不再丢失。
  - 优化：`vite.config.ts` 用函数式 `manualChunks` 把 echarts+zrender 拆为独立 vendor chunk，`chunkSizeWarningLimit` 提至 600，消除构建告警。
  - 地基：新增后端 pytest 套件（`backend/tests/`，33 用例，覆盖 scoring/timing/backtest/eastmoney 解析/repo 迁移）+ GitHub Actions CI（`.github/workflows/ci.yml`：后端 pytest + 前端 type-check/build）。
  - 文档：新建 `CLAUDE.md`（AI 方向锚点）与本计划文件。
  - 地基(前端)：引入 vitest，新增 `format` / `dca` 单测（28 用例），接入 CI（`ci.yml` 前端 job 加 `npm run test`）。推进 P0「前端单测」首批落地。
  - 地基(后端)：可观测性——`eastmoney`/`repo`/`main` 静默吞错改结构化 `logging`（主源降级 / 退陈旧缓存 / 启动导入失败可见），加降级日志测试（pytest 34 用例）。完成 P0「可观测性」。
  - 地基(后端)：数据源健壮性——`eastmoney.source_health()` 统计主源成功/失败/失败率/最近错误/degraded，经 `/api/health` 暴露，加 source_health 测试（pytest 35 用例）。完成 P0「数据源健壮性」。**P0 三项全部完成。**
  - 功能(P1/V3-5 步骤1)：指数估值数据管线打通——`enrich_index_valuation.py`（乐咕乐股 `stock_index_pe_lg`/`pb_lg`，funddb 已废）+ `fund_index_map.json` + `enrich.yml`。经 4 轮 CI 校准（接口名→symbol 全称→市值加权列），产出 6 个主流宽基真实 PE/PB+历史分位、数值合理。步骤2：后端按 Pages URL 加载、接入 timing 估值层 + 补创业板/科创/海外指数源。
